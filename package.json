{
  "name": "site-deploy",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "dev": "npm run services:up && npm run wait-for-postgres &&npm run migration:up && next dev",
    "services:up": "wsl docker compose --file infra/compose.yaml up -d",
    "services:up:ci": "docker compose --file infra/compose.yaml up -d",
    "services:stop": "wsl docker compose -f infra/compose.yaml stop",
    "services:down": "wsl docker compose -f infra/compose.yaml down",
    "lint:prettier:check": "prettier --check .",
    "lint:prettier:fix": "prettier --write .",
    "lint:eslint:check": "eslint . --stats",
    "test": "npm run services:up && concurrently -n next,jest --hide next -k -s command-jest -c \"none,bgMagenta.bold.black\" \"next dev\" \"jest --runInBand --verbose\"",
    "test:ci": "npm run services:up:ci && concurrently -n next,jest --hide next -k -s command-jest -c \"none,bgMagenta.bold.black\" \"next dev\" \"jest --runInBand --verbose\"",
    "test:watch": "jest --watchAll --runInBand",
    "migration:create": "node-pg-migrate --migrations-dir infra/migrations create",
    "migration:up": "node-pg-migrate -m infra/migrations --envPath .env.development up",
    "wait-for-postgres": "node infra/scripts/wait-for-postgres.js"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "async-retry": "1.3.3",
    "dotenv": "17.2.3",
    "dotenv-expand": "12.0.3",
    "next": "16.1.4",
    "node-pg-migrate": "8.0.4",
    "pg": "8.17.2",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "eslint": "9.39.2",
    "eslint-config-next": "16.1.4",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-jest": "29.12.1",
    "globals": "^17.1.0",
    "jest": "29.6.2",
    "prettier": "3.8.1"
  }
}
